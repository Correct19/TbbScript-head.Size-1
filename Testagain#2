local npcs = workspace.NPCFolders.EnemyFolder

while task.wait(0.2) do
for _, v in ipairs(npcs:GetChildren()) do
    local head = v:FindFirstChild("Head")
    if head then
        head.Size = Vector3.new(8, 8, 8) 
    end
end
